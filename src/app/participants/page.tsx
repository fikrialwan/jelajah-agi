import { ParticipantProcess } from "~/lib/components/pages/participant/process-page";

const ParticipantPage = () => {
  return (
    <section className="container mt-[50px] pt-8 h-screen">
      <ParticipantProcess />
    </section>
  );
};

export default ParticipantPage;
